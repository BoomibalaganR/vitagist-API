/**
 * Exports the IdentityDocument model.
 * 
 * This module exports the IdentityDocument model which is required from the 'identity' file.
 * The IdentityDocument model is used to interact with the identity documents in the database.
 * It provides various methods to perform CRUD operations on the identity documents.
 * 
 * @module models/IdentityDocument
 * @requires ./identity
 */

exports.IdentityDocument = require('./identity')
