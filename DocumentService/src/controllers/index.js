/**
 * Export the identity document controller.
 * 
 * This module exports the identityDocumentController which handles CRUD operations 
 * for identity documents. It includes methods for getting, adding, updating, and deleting
 * identity documents.
 * 
 * @module controllers/identityDocumentController
 * @requires ./identityDocumentController
 */
exports.identityDocumentController = require('./identityDocumentController')
